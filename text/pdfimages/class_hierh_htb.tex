\documentclass{standalone}
\usepackage{tikz}

\begin{document}

    \begin{tikzpicture}[sibling distance=10em,
						edge from parent/.append style = {line width = 0.1em},
						every node/.style = {shape = circle, draw, minimum width=1.2cm, color=blue, line width = 0.1em}]
		\node {\bf root}
			child { node {\bf filter}
				child { node {\bf TBF}}
				child { node {\bf TBF}}
			}
			child { node {\bf filter}
				child { node {\bf TBF}}
			}
		;
	\end{tikzpicture}

\end{document}

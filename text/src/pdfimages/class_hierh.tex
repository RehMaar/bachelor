\documentclass{standalone}
\usepackage{tikz}

\begin{document}

    \begin{tikzpicture}[sibling distance=10em,
						every node/.style = {shape = circle, draw, minimum width=1.2cm}]
		\node {1:0}
			child { node {1:10}
				child { node {10:21}}
				child { node {10:22}}
			}
			child { node {1:20}
				child { node {20:31}}
			}
		;
	\end{tikzpicture}

\end{document}
